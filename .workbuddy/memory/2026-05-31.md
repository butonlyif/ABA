# 2026-05-31 工作日志

## 用户手册 Word 版生成
- 将 ABA智能助手_用户手册.md 转化为 Word 文档（`.docx`），用户友善排版
- 美化表格：渐变色表头、交替行背景、统一排版
- 生成 5 张示意图（matplotlib，无 emoji 兼容版）：
  1. `01_product_overview.png` — 产品结构图（ABA 主应用 ↔ 生活教练）
  2. `02_quick_start.png` — 5 步快速上手流程
  3. `03_dtt_levels.png` — DTT 四级辅助记录体系
  4. `04_act_process.png` — ACT 六大核心过程环形图
  5. `05_growth_path.png` — 成长路径个性化议题驱动流程
- 图表保存在 `diagrams/` 目录，已嵌入 Word 文档
- 最终文档 402KB：`ABA智能助手_用户手册.docx`

## 技术备忘
- python-docx 在 managed Python 3.13 环境有 lxml 签名问题，改用系统 Python 3.10
- `RGBColor.__hex__` 不存在，需手动转 `f"{r:02X}{g:02X}{b:02X}"`
- matplotlib 中文 emoji 字体缺失，统一改用文字标签+彩色圆形替代

## 示意图修复（v2）
- 用户反馈：框体重叠、文字被截断、字符显示怪异
- 修复内容：
  - 图3 DTT四级辅助：增大画布宽度 9→14，减窄框体 1.95→2.7，增大间距 2.2→3.2
  - 图5 成长路径：增大画布宽度 9→14，步骤框增大间距，阶段框宽度 1.8 间距 2.2
  - 图1 产品结构：增大画布高度 4.5→5.2，增宽模块框 3.8 加大内容间距
  - DPI 从 150 提升到 200，提高清晰度
  - 所有图表字体统一，避免 emoji 字体缺失导致的方块问题
- 最终文档 620KB（比 v1 的 402KB 更大，图片质量更高）

## 示意图修复（v3 — 字体统一）
- 用户反馈：字大大小小、有的黑体有的没有，很难看
- 根因：matplotlib 默认 DejaVu Sans 不支持中文，回退渲染导致字重不一致
- 修复内容：
  - 配置 `matplotlib.rcParams['font.family'] = ['Noto Sans SC', 'Heiti SC', ...]`
  - 建立统一字体层级规则：标题 15pt bold、模块名 13pt bold、小节 11pt bold、正文 10pt regular、描述 9pt regular
  - 移除所有小字号 bold（<10pt），移除 text alpha 透明度
  - Noto Sans SC 位于 `/Users/wangxin/Library/Fonts/NotoSansSC.ttf`，matplotlib 可正常识别
- v3 图表已重新生成，Word 文档已重新嵌入
