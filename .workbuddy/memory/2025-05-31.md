## 2025-05-31

- 修复 life_coach_app.py 中的 API 调用错误（authenticate_user → login, register_user → register, get_user_by_username → 直接查 SQLite）
- 全面重写教练对话引擎 v2：
  - 情绪识别系统：8 类情绪（焦虑/悲伤/愤怒/疲惫/自责/孤独/困惑/感恩）的关键词检测
  - 教练策略库：每种情绪有独立的教练方法论（approach）、开场白（opening）、4-7 个跟进问题（follow_ups）
  - 连贯性对话：跟踪同一情绪主题的对话回合数，循环使用不同层次的跟进问题
  - 困惑情绪的苏格拉底式引导：价值观探索、五年后回望、优先级排列、小实验设计
  - 知识库引用：首次提到某情绪时推荐相关文章
  - 任务推荐：对话 2 轮后自动推荐教练任务
- 新增情绪教练任务系统（🎯 教练任务页面）：
  - 8 类情绪共 40+ 个教练任务（练习/反思/学习三种类型）
  - 根据最近对话和情绪记录自动推荐对应任务
  - 可浏览全部情绪的所有任务
  - 任务完成状态持久化保存
- 修复知识库文章链接无法打开的问题：
  - 首页快捷入口的 art_kw 不匹配实际文章 ID（如 "acceptance" → "acceptance_1"），已全部修正
  - 情绪类快捷入口改为直接发送消息到对话并触发教练回应（而非跳转到不存在的文章）
  - 聊天对话中推荐的知识库文章从纯文本改为可点击的按钮（通过 __KB_REFS__ 标记 + 消息渲染逻辑）
- 重构情绪追踪页面：
  - 新增「为什么要记录情绪」的解释（4个价值点）
  - 记录表单从简单的一句话改为详细表单（触发事件、情绪强度1-10、身体感受、想法、补充）
  - 历史记录增加趋势分析（平均心情、平均强度、常见触发模式、趋势箭头）
  - 情绪记录同时存入综合记录（personal_records）
- 完善成长路径6个阶段：
  - 每个阶段新增 guidance 指导内容（解释这个阶段的意义和方法）
  - 每个任务新增 detail 详情（怎么做、在记录中写什么）
  - 任务列表增加「详情」按钮可展开查看
  - 阶段完成时显示祝贺卡片，引导用户在记录中写总结
- 新增「我的记录」综合记录模块（替代原「成长日志」）：
  - 支持6种记录类型（随笔/感悟、情绪记录、练习记录、阅读笔记、反思总结、目标/计划）
  - 每条记录包含标题、内容、心情、标签
  - 支持关键词搜索和类型筛选
  - 支持查看全文和删除
  - 所有记录持久化到 coach_data.json
- 更新报告页面：
  - 新增「个人记录」统计卡片（6列数据）
  - 新增「记录类型分布」图表
  - 情绪趋势增加「情绪强度」维度和「常见情绪触发」分析
  - 新增「最近个人记录」预览（含标签）
  - 新增「最近教练对话」回顾
- 修复情绪记录模块严重 bug：用 selectbox 替代 button 选择心情，解决 Streamlit rerun 导致表单状态丢失的问题；保存成功提示通过 session_state 标志在 rerun 后正确显示
- 修复 THEME KeyError：代码引用了不存在的 warning/danger/success key，替换为 accent/warm/calm
- 修复成长路径「后面的阶段看不到内容」：改为所有阶段都可浏览内容和查看详情，只锁定未解锁阶段的「完成」按钮
- 重构进展报告页面：
  - 所有记录（情绪+个人记录+完成任务）按周分组，以 expander 折叠展示，每周显示摘要统计
  - 新增 AI 周报总结功能：选择某一周后点击按钮生成，包含情绪概况、记录活动、对话频率、个性化建议
  - 周报基于实际数据计算（平均心情、情绪强度、高频情绪、触发分析）生成建议
- 成长任务快速跳转功能：
  - 为全部28个成长任务添加 TASK_ACTIONS 映射（3种跳转类型：知识库文章、我的记录预填、情绪追踪）
  - 每个任务右侧显示快速跳转按钮（「📖 阅读文章」「📝 去记录练习感受」「😊 去情绪追踪」）
  - 跳转到知识库时自动打开对应文章，跳转到记录时预填标题和模板内容（如练习感受框架、反思问题列表）
  - render_journal 增加 journal_prefill 处理：自动选择记录类型、预填标题和内容，显示「来自成长任务」提示
  - render_knowledge 增加 kb_open_article 处理：自动打开指定文章，显示提示
- 重构成长路径为议题驱动模式（符合 ACT 教练理论）：
  - 新增 ACT 六大核心过程说明（可展开的科普卡片，2x3 网格展示接纳/解离/当下/自我/价值观/行动，并解释「为什么要围绕议题」）
  - 数据模型从单一进度改为多项目（growth_projects 列表，每个项目有 id/issue/stage/tasks_done/status）
  - 用户创建一个具体议题（如"孩子发脾气时的焦虑"）后围绕该议题走 6 个 ACT 阶段
  - 每个阶段的指导文字自动融入当前议题（📌 你的议题：xxx）
  - 完成全部 6 阶段后可写总结反思或开始新议题
  - 支持暂停/恢复议题、并行开启新议题
  - 已完成议题归档到「🏆 已完成」区域
  - 旧数据自动迁移为第一个项目（兼容 growth_stage/growth_tasks_done）
- 修复 session_state key 冲突：gen_weekly → _weekly_report_wk
- **成长路径个性化生成**：
  - 新增议题类型检测函数 detect_issue_type()，基于关键词匹配识别焦虑/自责/疲惫/社交/亲子关系5类议题
  - 新增 ISSUE_STAGES_TEMPLATES 模板库，每类议题6阶段×3-5个针对性任务（共约100+个个性化任务）
  - 每类任务的指导语、练习内容都针对该议题定制（如焦虑类有「焦虑身体地图」「4-7-8呼吸法」「焦虑冲浪」；自责类有「慈悲朋友视角」「给自己写信」）
  - 创建议题时自动检测类型并匹配个性化模板，项目头部显示类型标签（如🟡 焦虑方向）
  - 未匹配到特定类型时回退到通用 GROWTH_STAGES
  - 新增 get_project_stages()/get_project_total_tasks() 辅助函数，所有引用 GROWTH_STAGES 的地方（报告/统计/暂停列表/周报）已改为使用项目自定义 stages
  - 自定义任务自动生成通用快速跳转按钮（reflect/practice→去记录，track→去情绪追踪，learn→去知识库）
  - 新增「🔄 换一套练习」功能，用户可选择自动检测或其他类型模板重新生成练习路径
- 修复报告页 NameError：emotion_tasks_done 未从 session_state 取值
- **更新产品介绍 PPT（v1.3.0 → v1.4.0）**：
  - S1 版本号更新
  - S3 产品全景：生活教练描述更新（个性化路径、周报、情绪追踪）
  - S12 从 6 能力改为 8 能力（4×2 网格）：新增议题驱动成长路径、AI周报、教练任务、任务快速跳转
  - S13 从固定 ACT 六阶段改为议题驱动的个性化模板展示（焦虑/自责/疲惫/社交/亲子+通用）
  - S17 案例分享更新：体现议题创建→自动匹配模板→AI周报看到频率下降
  - S18 一天使用场景描述更新（情绪追踪详细记录、AI周报）
  - S19 如何开始第三步更新
- **编写用户手册**：
  - 创建 `ABA智能助手_用户手册.md`（44KB，详细文档）
  - 覆盖 ABA 主应用全功能（AI助手/孩子档案/入门评估/任务清单/训练记录/图片卡片/进展记录/数据看板/报告中心）
  - 覆盖生活教练全功能（首页/教练对话/知识库/教练任务/情绪追踪/成长路径/我的记录/进展报告）
  - 每功能含：界面说明、控件表格、操作步骤、注意事项、FAQ
  - 含产品概述、快速开始、专家后台说明、技术支持章节
