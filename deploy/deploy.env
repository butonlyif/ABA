# 部署敏感配置（不入库，由 deploy.sh source 并同步到服务器供 docker compose 插值）
# 本文件已在 .gitignore 中忽略。修改后重新跑 deploy.sh 即生效。

# ── SSH 连接 ──
SERVER_IP=170.106.143.145
SERVER_USER=ubuntu
SSH_PASS=Dwang220124
# 或改用密钥（设了 SSH_KEY 可不填 SSH_PASS）：
# SSH_KEY=/Users/you/.ssh/id_ed25519

# ── docker compose 插值变量 ──
# 主应用↔生活教练免密跳转签名密钥（重置：openssl rand -hex 32）
COACH_SSO_SECRET=4e81aeb7b381530436809b7aa14093897a78f856cf751ff3f513dcf8ce4cb9cd
# 用户浏览器加载教练 iframe 的公网地址
LIFE_COACH_URL=http://170.106.143.145:8503
# 「← 返回 ABA 助手」跳回的主应用公网地址
ABA_APP_URL=http://170.106.143.145:8501
# 容器内健康检查地址（用 compose 服务名，一般不用改）
COACH_HEALTH_URL=http://life-coach:8503/_stcore/health
